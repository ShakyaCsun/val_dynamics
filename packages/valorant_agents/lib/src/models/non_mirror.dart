enum NonMirror {
  /// Indicates this is a non-mirror matchup
  yes,

  /// Indicates this will be a non-mirror matchup if reversed
  yesIfReversed,

  /// Indicates this is never a non-mirror matchup
  no,
}
